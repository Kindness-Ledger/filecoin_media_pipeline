# Environment for Filecoin media pipeline
# IPFS API endpoint, for example https://api.web3.storage or your own node
IPFS_API_ENDPOINT=
# Token for the IPFS API if required
IPFS_API_TOKEN=
# Optional public gateway URL for retrieval tests
GATEWAY_URL=https://w3s.link
